# Controllers

Place controller files here. Each controller handles the business logic for a specific resource.

## Convention

- One file per resource (e.g., `machines.controller.ts`)
- Export named async functions consumed by route handlers
